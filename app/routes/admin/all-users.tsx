import { Header } from "components";
import React from "react";

const Users = () => {
  <main className="dashboard wrapper">
    <Header title={`Welcome "Guest"} `} des={`Assalomu aleylum`} />
    Dashboard page
  </main>;
};

export default Users;
